import { Header } from "src/shared";

import classes from "./home.module.css";

export const Home = () => {
  return (
    <div className={classes.home}>
      <Header />
      HOME
    </div>
  );
};
